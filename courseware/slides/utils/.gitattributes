# These files are copied into / executed inside a Linux container and MUST use
# LF line endings, even when checked out on Windows. CRLF would break the
# shebang and the sed/node/marp-cli invocations.
entrypoint.sh   text eol=lf
preprocess.mjs  text eol=lf
convert.sh      text eol=lf
Containerfile   text eol=lf
